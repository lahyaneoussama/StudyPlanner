
# 📘 دليل تشغيل تطبيق مخطط الدراسة (Study Planner)

مرحباً بك! لكي يعمل المشروع بشكل صحيح على جهازك، اتبع هذه الخطوات البسيطة:

## الخطوة 1: قاعدة البيانات (MySQL) 🗄️
1. شغل برنامج **XAMPP** أو **MySQL**.
2. أنشئ قاعدة بيانات باسم `study_planner_db`.
3. استورد (Import) ملف `backend/database.sql` أو انسخ محتواه وشغله في SQL Query.

## الخطوة 2: تشغيل الخلفية (Backend) 🚀
1. افتح Terminal في مجلد المشروع.
2. ادخل لمجلد الـ backend: `cd backend`.
3. ثبت المكتبات: `npm install`.
4. عدل بيانات الاتصال في `config/db.js` (اسم المستخدم وكلمة السر).
5. ابدأ الخادم: `node server.js`.

## الخطوة 3: تشغيل الواجهة (Frontend) 💻
1. افتح Terminal ثانية في مجلد المشروع الرئيسي.
2. ابدأ التطبيق: `npm run dev`.
3. افتح الرابط الذي سيظهر لك في المتصفح.

---
**ملاحظة:** تأكد دائماً من تشغيل الخادم (الخطوة 2) قبل محاولة تسجيل الدخول في التطبيق.
